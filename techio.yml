title : Rust

# Table of contents
plan:
  - title: Welcome # Set the page title
    statement: markdowns/welcome.md # Set the file path to the page content
#  - title: Another Page
#    statement: markdowns/another-page.md

# Settings for your programming projects and the associated Docker images
projects:
  rust:
    root: "/rust-project"
    buildCommand: cargo build
    # Set the docker image. This image runs a command line directly using this syntax, for instance:
    # @[Code Editor Title]({"stubs": ["src/lib.rs", ...], "command": "cargo test --release test_universe"})
    runner: techio/rust:latest
